name: GitHub Stats Cache

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  cache:
    runs-on: ubuntu-latest
    steps:
      - name: Cache GitHub Stats
        run: echo "Stats cache refreshed"
